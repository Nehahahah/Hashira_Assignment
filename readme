This project solves the assignment using Go.
It reads roots from JSON, decodes different bases, and applies Lagrange Interpolation to find the constant `C`.
1. Install Go (`go version` to check).
2. Clone repo & enter folder.
3. Run with an input file:
   go run main.go input1.json
